import { Component } from '@angular/core';

// Formulario pendiente — usa AuthService.nuevaPassword() con el token de la URL.
@Component({
  selector: 'app-nueva-password-page',
  templateUrl: './nueva-password.page.html',
  styleUrl: './nueva-password.page.scss',
})
export class NuevaPasswordPage {}
